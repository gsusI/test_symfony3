{% extends 'base.html.twig' %}

{% block body %}
    <h1>Player list</h1>

    <table>
        <thead>
            <tr>
                <th>Playerid</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for player in players %}
            <tr>
                <td><a href="{{ path('player_show', { 'id': player.playerID }) }}">{{ player.playerID }}</a></td>
                <td>{{ player.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('player_show', { 'id': player.playerID }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('player_edit', { 'id': player.playerID }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('player_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
